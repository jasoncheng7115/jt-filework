//! Workspace-level conformance tests.
//!
//! This crate has no runtime code. It exists so `tests/architecture.rs` and
//! `tests/i18n.rs` can run against the repository as a whole rather than from
//! inside one crate, which is what `docs/TESTING.md` §3 requires.
