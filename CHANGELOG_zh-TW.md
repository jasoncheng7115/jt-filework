# 變更紀錄

jt-filework 的重要變更都記錄在這裡。

格式依照 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/)，版號依照
[語意化版本](https://semver.org/lang/zh-TW/)。主版號還是 0 的期間，介面與工作階段
（session）格式仍可能在次版號之間變動；工作階段檔案本身帶有格式版號，並會自動
向前遷移（`docs/UPGRADE.md`）。

英文版與本檔並存於 [`CHANGELOG.md`](CHANGELOG.md)。兩份都是手寫的，同一次變更必須
同時更新兩份。

## [0.6.0] - 2026-09-01

### 新增

- **磁碟用量視窗可以直接動手。** `C` 複製、`M` 移動、`D` 移到回收筒，對象是游標
  所在的那一列，右鍵選單也有同樣三項。找出吃掉磁碟的那個資料夾之後，還得換到別
  的地方才能處理它，那趟走訪等於白走了大半。

  操作完成後那一層會重新統計——這份報告講的是一個剛剛變過的資料夾，繼續顯示舊
  數字就是拿它們冒充現況。面板也會一併重新整理，因為從報告裡丟掉的檔案，在面板
  看到的資料夾裡也已經不在了。

  這裡的操作由呼叫端指名來源路徑，而不是讀某個面板的選取（`jtf_op_prepare_paths`）：
  這個視窗是一份關於資料夾的報告，不是一個正在顯示資料夾的面板，邊界後面沒有選
  取可讀。

- **磁碟用量視窗有了熱鍵提示列**，用的是跟檔案清單、檢視視窗同一種鍵帽：
  `→ 進入 · ← 上層 · C 複製 · M 移動 · D 回收 · Tab 換一邊 · Esc 關閉`。它從長出
  那三個命令的那一刻起，就不再是只能讀的報告；而一個按鍵不在畫面上的視窗，沒有
  人會知道它有按鍵。

- **磁碟用量視窗可以用方向鍵走。** 右鍵與 Enter 進入、左鍵與 Backspace 回上層、
  Tab 在兩個清單之間切換——跟檔案清單走資料夾樹是同一套走法。進到一層之後游標會
  停在第一列，才有地方可以再按右鍵。

- **兩種走訪都會說走到哪了。** 搜尋進行中時，狀態列顯示正在搜尋的資料夾；磁碟用
  量視窗則顯示在自己狀態列的尾端。數字只說明走訪還活著，說不出走到多深，而
  「32,894 個檔案」在 `Downloads` 裡和在某個 cache 深處是同一個數字。路徑才是決
  定要等下去還是縮小範圍的那個資訊。

- **側邊欄每個磁碟都有一條用量指示計**，依使用率變色：75% 以下是強調色、到 90%
  轉琥珀、超過就是紅色，確切數字在滑鼠停留時顯示。磁碟區那幾列本來只說明有哪些
  磁碟，對它們本身一句話都沒說，而「快滿了沒」正是一顆磁碟唯一會變的事。它掛在
  原本偵測磁碟插拔的那個計時器上更新，所以那是現在的數字，不是你上次剛好導覽過
  去時的。

- **所有的刪除都會先確認**，不管是從選單、熱鍵，還是磁碟用量視窗發動的。永久刪
  除維持原本較強的警告，回收筒則是比較平的一句問話。回收筒是可以復原的，但仍然
  值得問：問題不只是資料活不活得下來，而是這個人是不是真的想按那個鍵——在這套鍵
  盤上，`D` 就在 `S` 和 `F` 旁邊。

- **程式在三個平台上都有視窗圖示**，「關於」也會顯示它。macOS 讀 `.icns` 給 Dock
  和 Finder，而 Qt 兩個都不讀，所以 `QApplication::windowIcon()` 一直是空的，
  「關於」視窗左上就開了一個空白方格。

- **最近使用的位置可以從自己的右鍵選單加入書籤**，而且只在它還不是書籤時才出現。

### 變更

- **側邊欄固定顯示，只有資料夾樹可以收合。** 原本那個命令把側邊欄整個藏起來，特
  殊位置也跟著不見了；但那是側邊欄裡固定的一半：書籤、伺服器、磁碟區、剛去過的
  地方，都不隸屬於當下打開的那個資料夾。

- **熱鍵提示列改用短名**——`複製`、`移動`、`回收`、`標記`——選單則維持完整名稱。
  選單上寫「移到資源回收筒」，是因為那才是這個命令做的事，而且旁邊還有一個「永
  久刪除」；但要塞十幾個項目的提示列要的是「回收」。`F5 重新整理` 和 `A 屬性`
  移出提示列了：重新整理是東西看起來不對時才按的鍵，卻在每個畫面上都佔著寬度。
  詳細度設定現在是「顯示按鍵與名稱」與「只顯示按鍵」，後者比以前真的更精簡。

- **狀態列把寬度讓給左邊。** 右邊那幾個計數的內距少了一半，沒有內容的計數直接隱
  藏而不是留著空白和分隔線，左邊的訊息則會自己縮、從中間省略，不再要求整段文字
  的寬度——這樣將來放一條長路徑進去，也不會把右邊那組推出畫面。

- **狀態列的按鈕寫「快速鍵參考表」**，不再附帶目前模式。工具列的模式切換在幾公
  分外就說了同一件事；而一個標籤有一半是狀態的按鈕，看起來就像唯讀的狀態列，沒
  有人會去點。字型也改回介面字型：工具按鈕會套用樣式自己那套比較小的字，結果整
  列裡唯一可以點的東西字最小。

- **單鍵命令模式改稱「全鍵模式」**（繁體中文介面）。

- **「關於」視窗**列出作者、連到正確的原始碼位置，也不再用另一套程式來描述這支
  程式。

- **檢視視窗**的熱鍵提示改用跟程式其他地方一致的鍵帽，取代原本一行粗體字母；頂
  端工具列與內文分開，搜尋框加了放大鏡；閱讀區也有了邊距。

- **側邊欄選取的那一列是一整條膠囊**，橫跨整列繪製。

### 修正

- **檢視視窗的搜尋命中被畫成黑色方塊。** `setHighlight` 只有檔案清單呼叫過，檢視
  視窗從來沒有，所以 delegate 裡是兩個預設建構的——也就是無效的——顏色，而無效顏
  色填出來就是黑的：黑底黑字，正好蓋在你要找的那個詞上。

- **Escape、Enter、Tab 被從輸入欄位手上搶走。** macOS 的選單列是整個應用程式共用
  的，而鍵盤配置把這三個都綁掉了（`escape` 是 `search.clear`、`enter` 是
  `file.open`、`tab` 是下一個面板），所以它們比有焦點的欄位先拿到鍵。篩選框裡的
  Escape 什麼都沒清，Tab 跳去隔壁面板而不是把鍵盤交給篩選後的清單，Enter 則直接
  開檔案。現在「正在打字」這條規則會把它們判給有焦點的欄位，就像它原本就會判給
  字母那樣。

- **在篩選後的清單上按 Escape 沒有反應。** 它綁的是 `search.clear`，只清搜尋不清
  篩選。兩者都是在縮小清單，Escape 的意思就是「別再縮了」，不管當下生效的是哪
  一個。

- **同一個選單列的衝突也吃掉了磁碟用量視窗的左右鍵**，這就是為什麼 Backspace 可
  以回上層而左鍵不行。

- **伺服器的「重新連線」從來不會跳出密碼詢問。** 那個詢問只從整批重新整理時發
  動，而登入是在背景執行緒上失敗的：按下按鈕的當下還沒失敗，等真的失敗時，注意
  到的那一拍只重新整理了列與狀態列。現在每一拍都會檢查。

- **側邊欄啟動時佔掉半個視窗。** 還原寬度那段程式碼住在「資料夾樹變成可見」裡
  面，而那件事已經不再等於「側邊欄出現」；樹收起來啟動時就完全沒有人套用寬度，
  接著只要拖動任何一條分隔線，版面自己猜的那個結果就被存成了偏好設定。現在只有
  分隔線真的拖得到的寬度才會被記住。

- **退出鈕讓側邊欄每一列都少了名稱的尾巴。** 樹狀元件的欄寬是所有列共用的，所以
  一顆按鈕、在一顆卸除式磁碟那一列，帳卻算在書籤、伺服器和最近使用頭上。磁碟區
  那一列上的兩個控制項現在都畫在名稱欄裡面。

- **側邊欄的選取光棒四個角有黑色缺口。** 樹狀元件會自己把選取的子項目左邊那塊縮
  排區填成 palette 的強調色，是直角的；而圓角膠囊從項目本身開始畫，兩個形狀在中
  間相接，膠囊的圓角就把後面那塊直角方塊咬掉四個角。

- **磁碟用量視窗中，選取那一列的比例長條會消失**，因為長條用的強調色在那一列上
  正好就是選取的背景色。

- **對話框圖示在深色主題下看不清楚。** `QMessageBox::Question` 之類是由樣式用它
  自己的顏色畫的，深色背景上就是黑底黑問號。現在改用我們自己的圖示、取 palette
  的顏色——而且畫的是即將執行的那個動作，比一個標點符號說得更多。

- **磁碟用量視窗「依類型」的圖示全是同一個通用文件。** `QFileIconProvider` 回答
  的是「磁碟上某個檔案」，而一個講「類型」的列並沒有檔案可以指。現在改成向平台
  詢問類型。

## [0.5.0] - 2026-08-31

### 新增

- **磁碟用量分析。** 檔案選單與資料夾的右鍵選單裡的「分析磁碟用量…」會掃描一
  個資料夾，用一次走訪回答兩個問題：哪個子分支佔最多，以及**哪一類檔案加起來
  最多**。後者是市面上這類工具大多回答不了的，而它有一半以上的時候才是比較有
  用的那個——「照片佔了 40 GB」現在是個有答案的問題。

  每一列都有一條占比長條，因為一整欄的位元組數字，比較這件事還是要讀的人自己
  做。在資料夾列上按兩下就會過去，那正是找到它的目的。直接放在該資料夾裡的檔
  案有自己的一列，這樣各分支才會加總回總計，而不是留下一個看起來像錯誤的差
  額。

  它跑在背景執行緒上，有即時計數與可用的取消，並且在結果不完整時會說出來——
  悄悄略過讀不到的資料夾，比標明「不完整」更糟。符號連結不追也不算：連回上層
  會繞不完，而連到別處的大檔並不是這裡用掉的空間。

  對一個真實的 23.8 GB 資料夾實測：80,460 個檔案、9,670 個資料夾，2.8 秒，兩
  份細目都精確加總回總計。

- **路徑欄會邊打邊補齊**（`P`，CView 鍵盤裡的 `\`）。補齊的來源是樹狀圖列出
  資料夾用的同一支呼叫，而不是 Qt 自己的檔案系統模型——資料夾裡有什麼只有一個
  來源，而且伺服器上的路徑也能補，那是 Qt 的做不到的。

- **熱鍵提示列加入切換面板。** 開兩個面板時，那是方向鍵之外最常按的一個鍵，卻
  是提示列唯一沒提過的常用鍵。只有一個面板時不顯示，因為替一個按了沒作用的鍵
  做提示，只會讓人不再相信這條提示列。

## [0.4.0] - 2026-08-31

### 新增

- **ISO 映像檔可以像壓縮檔一樣開啟**（ADR-0005）。在 `.iso` 上按 Enter 會列出
  裡面的內容，`C`／`X` 把成員取出來，用的是與 ZIP 相同的按鍵與相同的視窗。支援
  ISO 9660、Joliet（映像檔有的話優先採用，因為那是製作者希望大家看到的名稱），
  以及 Linux 映像檔常帶的 Rock Ridge `NM` POSIX 名稱。

  這個讀取器是自己寫的，沒有引入套件：現有的幾個都是維護稀疏的二進位剖析器，
  而我們要的性質不是「能不能剖析正常的映像檔」，而是「遇到壞掉的會怎樣」。每
  一個 extent 都先對照檔案的實際長度檢查才讀，走訪用佇列並帶深度上限與已讀集
  合，所以目錄成環會結束，不會用檔案自己給的數字配置記憶體，而會爬出目的資料
  夾的名稱由與 ZIP 相同的函式拒絕。

  不讀 UDF。只有 UDF 樹的映像檔會回報無法讀取，而不是列成空的——這是兩個不同
  的答案，而且只有一個是真的。

  任何東西都不會寫進映像檔。

- **光碟映像檔在種類欄是自己的類別**，不是「壓縮檔」。以第 32769 位元組的
  `CD001` 簽章判斷，不看副檔名，所以一個其實是映像檔的 `.img` 也算。

### 變更

- **壓縮檔、ISO 與資料夾比較三個視窗改用與面板檔案清單相同的樣式**：一樣的列
  高、字型、標題列、圖示與勾記。先前是三個長得不一樣的表格，看起來像別人的對
  話框而不是這個程式的一部分。

- **Qt 的建置目錄移出原始碼樹**，改到 `~/.cache/jt-filework-qt`，與 Rust 的
  target 目錄放在一起。這份 checkout 位於同步資料夾內，而建置輸出有 4.2 GB 的
  目的檔會在每次建置後重新上傳。可用 `JTF_BUILD_ROOT` 覆寫。release 建置同時
  會安裝到 `/Applications/jt-filework.app`，也就是 Dock 釘住的那個路徑。

### 修正

- i18n 測試現在會走過 `ContentKind::ALL`。`label_key` 回傳的是字面值而不是呼
  叫 `tr_`，原始碼掃描看不到那些 key，所以新增一個種類卻忘了字串時，使用者會
  看到原始的 key。已用刪掉一個 key 讓測試失敗的方式驗證過。

## [0.3.0] - 2026-08-31

### 新增

- **比較進行中會說明進度**，也可以中止。它本來就跑在背景執行緒，但在跑完之前
  什麼都不說，從外面看跟當掉沒有兩樣。現在會即時回報已讀資料夾數、項目數與差
  異數，按取消則保留已經找到的結果。

- **「將此資料夾加入書籤」與「在新視窗開啟」**出現在所有能指到資料夾的地方：
  路徑列、分頁、特殊位置清單的項目、樹狀圖，以及檔案清單裡的資料夾。樹狀圖先
  前根本沒有右鍵選單。

- **拖放時會問要移動還是複製**，不論來源是另一個面板還是別的程式。Qt 是依平台
  的修飾鍵慣例決定動作的，所以同一個手勢在同一顆磁碟內是移動、跨磁碟是複製，
  而畫面上沒有任何地方說剛才是哪一種。來自別的程式預設複製，來自自己的面板預
  設移動。

### 變更

- **比較的兩側改以資料夾名稱標示**，不再寫「左」「右」。面板可以上下分割，那
  時候左右什麼也沒指到。

- **文字預覽只索引檔案的前 4 MiB，檢視視窗 64 MiB。** 原本兩者都沒有上限：行
  索引是每行八個位元組，而建立索引要讀過整個檔案，所以預覽一個好幾 GB 的日誌
  會把整份讀完、先吃掉幾百 MB 的偏移表，才顯示第一行。

- **磁碟區與裝置合併為一區。** 為了分開兩列而多出兩個標題並不划算，而可卸除的
  那幾列本來就是唯一帶退出按鈕的。

### 修正

- **其他資料夾的標記被算成這裡的選取。** 標記會在離開資料夾後保留，所以那個集
  合裡有其他資料夾的項目——而狀態列把它們算進去了，標題列的勾選框在一個沒有任
  何勾選的資料夾裡顯示全選，「複製路徑」在只選一列時貼出三個路徑。更嚴重的
  是，複製或移動會動到畫面上看不到的檔案。計數與操作現在只看面板正在顯示的
  列。

- **拖曳未被選取的那一列時，拖走的是原本的選取項目。** Qt 是用選取的列組出拖
  放內容的；現在按下時會先選取該列，跟每個檔案管理器一樣。

- **分享面板彈出的位置差了一個半的面板。** 它用了視圖的 `frame`（視圖在父視圖
  中的位置）而不是 `bounds`。

- **在路徑列空白處按右鍵**會跳出 Qt 自己的復原／剪下／貼上選單，是英文的，而
  且講的是一個使用者看不到的文字欄位。現在跳出的是與路徑片段相同的選單，對象
  是面板目前所在的資料夾。

- 佈景主題、字型、鍵盤配置、語言四個子選單沒有圖示。

## [0.2.0] - 2026-08-31

### 新增

- **資料夾差異比較。**「檔案 ▸ 比較資料夾…」把焦點面板的資料夾與複製目標面板
  的資料夾並排，列出差異：只在左側、只在右側、不同，或相同。子資料夾預設不
  納入，勾一下就展開；相同的項目預設隱藏——一份大多數列都寫著「相同」的清
  單，會把真正要看的那幾列埋掉。

  兩個檔案在大小相同、時間相差兩秒以內時視為相同；兩秒是 FAT 的時間精度，也
  是跨檔案系統複製後常見的落差。這條規則印在表格下方，而不是讓人自己假設：
  大小與時間相同並不等於內容相同，這個視窗不該讓人以為它是。

  比對在背景執行緒進行，所以兩棵大目錄樹——或兩個在伺服器上的資料夾——不會讓
  視窗停止重繪。只存在於一側的資料夾是一列，不是底下每個檔案各一列；符號連結
  是一個名字，不是一道門；讀不到的子資料夾只中止那一支，而讀不到的根目錄則是
  錯誤，不會硬說「全部只在另一側」。

- **每個面板都有關閉按鈕**，就在頁籤旁邊；**開啟失敗的面板則多一個重新連線
  按鈕**。重試會先丟掉 provider 手上那條連線，所以失敗後按下去是真的重連，
  而不是再看一次同一條壞掉的連線。

- **側邊欄兩半各有標題欄**（特殊位置、資料夾）。先前兩份資料夾列表直接接在一
  起，看起來像一長串；兩者的背景色也統一了。

### 變更

- **樹狀圖會跟著焦點面板進到伺服器。** 伺服器在檔案系統旁邊有自己的根節點，
  所以停在 `sftp://…` 的面板，樹狀圖有地方可以顯示，而不是清空選取。它只會展
  開已經登入的伺服器——從介面執行緒列出未登入的伺服器就會在那裡登入，而登入要
  花多久由網路決定。

- **視窗標題與樹狀圖現在會跟著焦點走**，不再只在重新整理時更新。切到另一個面
  板時，兩者都還指著你已經離開的那個面板。

- **取消標題列的勾選框，光棒也會一起熄掉。** 選取就是標記；把每個框都取消卻讓
  每一列都還亮著，是同一件事的兩半在互相矛盾。

### 修正

- **頁籤上的關閉記號看不到。** 三件事同時出錯：`xmark.svg` 其實是
  `xmark-circle.svg` 的複本，那個叉只佔圖示框的四分之一；要求的尺寸不是圖示實
  際繪製的尺寸，Qt 只好把點陣圖縮小，筆畫也跟著變細；而樣式表的 margin 與
  padding 是從按鈕自己的框裡扣的，24px 的按鈕只剩 7px 可以畫。

- **標題列的全選框比資料列的勾選框偏左三個像素**，也小了一號。現在改成向樣式
  詢問資料列的勾選框畫在哪，而不是用 8 和 14 猜。

- **Dock 上的圖示開的是舊的 debug 版**，而 release 版在旁邊另外執行——同一個
  程式兩個圖示，因為 macOS 是以 bundle 路徑分組的。release 建置現在會安裝到
  `/Applications/jt-filework.app`，那才是值得釘在 Dock 上的路徑。

- 版本號由 CMake 從 `Cargo.toml` 讀取，不再在 `CMakeLists.txt` 裡另寫一份——
  第二份只會走樣。

## [未發布]

### 新增

- **壓縮檔的解壓縮與建立**(ADR-0003,已決議並實作)。在 ZIP 上按 `Z` 會先問要解到
  哪裡,再解壓過去;`Alt-Z` 把已標記的項目(或光棒所在的項目)壓成你指定的 ZIP。兩者
  都跑在背景執行緒、有即時計數、取消鈕真的有用——把大壓縮檔解在 UI 執行緒上會讓整
  個視窗凍住。**沒有百分比進度條**:每個成員的大小要等它真的出來才知道,顯示一個比
  例等於是編出來的。

  每個成員都會對目的資料夾解析路徑,只要會落到外面就拒絕,不管它怎麼寫——`../`、絕
  對路徑、Windows 磁碟機代號、UNC 前綴都算;而且反斜線在**所有平台**都當成分隔符,
  這樣一個在 Windows 上做出來的壓縮檔,不會在那邊是路徑穿越、在這邊變成一個無害的
  怪檔名。符號連結成員一律拒絕而不是建立。被拒絕的數量會明講出來。解壓縮的上限是
  單一成員 8 GiB、總計 32 GiB,而且是**對照實際出來的位元組**而不是檔頭宣稱的——檔
  頭說謊正是 zip bomb 的手法。取消解壓縮會把寫到一半的檔案刪掉。

  `zip` 釘在 `deflate` 這個 feature,因為它選的是 `flate2` 的純 Rust 後端;預設的
  feature 集會把 bzip2、lzma、xz、zstd 全拉進來,那些都是 C。

  在 ZIP 上按 `Enter` 會**另開一個視窗**列出內容,這正是 `CV.HLP` §四 的做法。用獨
  立視窗而不是面板,是因為壓縮檔不是資料夾——你不能進去、不能在裡面新增、也不能把
  東西拖進去,做成面板等於同時承諾了這三件事。視窗裡 `C` 解壓選取的項目、`X` 解壓
  全部,和 CView 的區分一致。路徑會跑到目的地之外的成員會被列出來、標示出來,滑鼠停
  留還會說明它解壓時會被拒絕——反正一定會被拒絕,而讓你**看見**它正是列表存在的意
  義。副檔名是 `.zip` 但其實讀不出來的檔案,會退回用一般方式開啟,而不是給你一個空
  視窗。

  從既有壓縮檔中刪除成員是**刻意不做**的:那要就地重寫整個檔案,中斷就可能毀資料。
  在視窗裡檢視或執行單一成員,需要先把它解到暫存檔、還要有人負責那個暫存檔的生命週
  期,所以 §四 的那兩個鍵是**沒有**而不是按了沒反應。
- **選取就是標記。**被反白的就是被打勾的,不管你是用滑鼠、用 Shift 加方向鍵,還是用
  `Space` 選的。在此之前兩者是分開的狀態,所以清單上可能出現五列反白、只有一個打勾,
  而且無從得知下一個命令會作用在哪一組。`AGENTS.md` §10 原本規定兩者要分開,現在改
  成相反,並寫明了理由。

  標記仍然是**被儲存**的那一份——session 存的是它、操作讀的也是它——而選取會在進入資
  料夾時從標記還原,所以「離開再回來標記還在」沒有被犧牲。`Space` 現在是把該列加進
  選取並往下移,那本來就是它的意思。
- **面板之間的分隔線拖不動。**分隔把手的樣式被設成 1 像素寬,而 Qt 的可拖曳範圍就
  等於把手的寬度——所以要在一個像素內點中才拖得動,實際上等於拖不動。現在是 7 像素
  寬,兩側用面板底色填掉,看起來仍然只是一條線。
- **作用中面板的外框從來沒有被畫出來過。**它從一開始就寫在樣式表裡,但普通的
  `QWidget` 子類別除非主動要求,否則會完全忽略樣式表的背景和邊框——所以那個用來指出
  「鍵盤在哪個面板」的標示,一直什麼都沒畫。現在面板會要求繪製、也留出邊框的空間,而
  且改成整圈外框,不再只是上緣一條線。

  外框用的是調色盤裡的面板指示色,不是選取色——而這正是要在兩個主題下都成立的關鍵:
  外框是襯著它後面的面板來讀的,而那兩個底色是相反的。調色盤給的答案是白底配較深的
  藍、近黑底配較淺的藍;實測對面板是 5.0 與 7.3、對非作用中面板的邊框是 3.8 與 5.7,
  都高於非文字指示元件所需的 3:1。現在只要有人調色盤改到低於這個門檻,測試就會失敗。
- **垂直分割的按鈕畫成三欄。**「垂直分割」是把兩個面板上下堆疊,它的圖示卻用了
  `view-columns-3`——欄數不對、方向也不對。現在改成被一條橫線分開的方框,與水平分割
  的直線相對。
- **名稱欄的標題加了全選勾選框。**每一列都有核取方塊,那一整欄的頂端本來就該有一
  個代表「全部」的;它會顯示全選、未選或部分選取,也會跟著其他地方做的標記變動。模
  型從一開始就在發 `markChanged`,只是沒有任何人在聽。
- **釘選分頁做好了卻碰不到。**釘選的分頁會保持在前段、不能被重新排序出去、也不能在
  未強制的情況下關閉——全都實作了,卻沒有任何地方可以釘。現在分頁右鍵選單和檔案選單
  都有,而且被釘選的分頁在分頁列上有標示。
- **有兩個組合鍵按了完全沒反應。**`Cmd-Shift-D` 綁著 `tab.duplicate`、
  `Cmd-Shift-F` 綁著 `search.ai`,兩個都列在快速鍵視窗和指令面板裡,但背後都沒有處
  理函式——按下去什麼都不會發生,這只會讓人學到「有些鍵就是壞的」。複製分頁的功能其
  實做好了,只是僅能從分頁列的右鍵選單用,現在接上了;AI 搜尋還沒做,所以那個組合鍵
  先拿掉,等功能做好再回來。現在只要有任何綁了鍵的指令在介面裡沒有處理函式,測試就
  會失敗——`file.edit` 從一開始就是這個毛病。
- **伺服器拒絕登入時,沒有任何地方可以補輸入密碼。**連線對話框只問一次,之後不論是
  還原 session、重新連線,還是連線中斷,都會走到失敗而無從補救——面板只說「你沒有執
  行這項操作的權限」,然後就沒了。現在它會詢問密碼並重試,而且是每次失敗問一次、不是
  每次重繪都問;只在伺服器拒絕**登入**時才問,因為帳號真的讀不到的資料夾不是密碼能解
  決的。輸入時會遮蔽,而且那是獨立的一支呼叫,不是在一般文字輸入上加個旗標。
- **點一下已儲存的伺服器,視窗會凍住直到放棄為止。**連線是在呼叫端執行緒——也就是
  UI 執行緒——上建立的,為的是讓錯誤的位址能立刻回報,而不是透過那條本來就在傳遞失
  敗的通道。但當那台機器根本沒開時,「立刻」就是連線逾時的二十秒,整段期間視窗停止
  重繪。現在連線改在 worker 上建立;錯誤晚一點才到,但那個代價遠比凍住的視窗小
  (`AGENTS.md` §3)。
- **從來沒有任何地方告訴核心「光棒停在哪一列」。**`Tab::active_entry` 是模型裡實際存
  在的欄位——session 會存它,操作在「沒有標記也沒有選取」時會退回用它——但介面裡沒有
  任何一條路徑會設定它,所以它永遠是空的,凡是「光棒指著什麼」的問題都得不到答案。
  在壓縮檔上按 `Enter` 沒反應,`Z` 在標記裡沒有資料夾時也找不到光棒所在的資料夾。現
  在面板會在光棒移動時回報。
- **每一個單字母命令,連 `Shift`+該字母也會被觸發。**Qt 對單一字母的快速鍵會連帶匹
  配加了 Shift 的形式(兩者產生的文字都是同一個大寫),而且快速鍵的處理**早於**焦點
  元件看到按鍵。所以 `Shift-H` 會開 16 進位檢視器、`Shift-C` 會複製、`Shift-M` 會移
  動,下面那個 Shift+字母跳躍也就永遠輪不到它自己的程式碼。檔案清單現在透過
  `ShortcutOverride` 先聲明這些組合鍵——那正是「這個鍵是我的」的標準機制。
- **勾了「等寬字型」,字型清單卻仍列出比例字型**,而且完全看不出哪個等寬字型比較窄
  ——但那正是想擠進更多欄位時唯一在意的事。現在勾了等寬就只列等寬家族,每個旁邊標
  出數字的寬度。
- **`Shift` + 字母或數字會跳到第一個以它開頭的項目**,再按一次跳到下一個——這是
  `CV.HLP` §二 的 `Shift-A`–`Z` 與 `0`–`9`。這是 CView 自己對「字母都拿去當命令了」
  的解法,也是單鍵模式在關掉輸入即找之後還能瀏覽的原因。`Shift-C` 和 `Shift-M` 原本
  是複製/移動到另一個面板,但那兩個是我們自己加的、不是 CView 的,所以讓位:複製保
  留 `C` 與 `Ins`,移動保留 `M` 並改用 `Shift-Ins`。
- **`H` 以 16 進位檢視檔案**——`CV.HLP` §二 的「以 HEX 16 進制方式觀看檔案」。檢視器
  一直都有 16 進位模式,只是沒有任何地方會打開它。
- **`E` 可以編輯了。**它從一開始就綁在鍵表上、也列在熱鍵提示列裡,但背後**根本沒有
  處理函式**,按了完全沒反應。現在會把檔案交給系統開啟純文字的預設程式;而且用 `O`
  新增的檔案會直接開起來編輯——這正是 CView 的 `Alt-O`(呼叫 ce.exe 建立或編輯檔案)
  一直在做的事。

- **SFTP 第一階段**（ADR-0004，已採納）：新增 `Location::Remote`、對照
  `~/.ssh/known_hosts` 的主機金鑰驗證、以 ssh-agent 或 `~/.ssh` 金鑰檔驗證的 SSH
  連線，以及透過與本機檔案系統相同的 `Provider` 介面列出遠端目錄的 `SftpProvider`
  ——因此排序、篩選、搜尋與逐批送出資料列都不必修改就能用於遠端。主機金鑰若變更，
  會顯示兩邊指紋並拒絕連線，且無法略過；未知主機在使用者接受前一律拒絕，接受後寫入
  `known_hosts`，而非本程式自己的儲存區。全程不詢問、不保存、不寫入任何密碼。C++
  遠端寫入操作已在函式庫層完成並對真實伺服器測試：建立與刪除目錄、刪除檔案、
  改名、含進度與取消的上傳下載。刪除**非空目錄**一律拒絕——SFTP 沒有遞迴刪除，
  這一層不會自己發明一個。30 秒的請求逾時讓「伺服器停止回應」成為明確錯誤而非
  卡死；這個機制是在一條故障網路線讓所有超過 64 KB 的傳輸都停滯時寫的，並在整段
  期間維持程式可用（`docs/TESTING.md` §5.3.1）。介面尚未呼叫寫入端。
  介面已可使用：**前往 → 連線到伺服器…** 輸入主機、連接埠、使用者、資料夾，以及
  ——只有在伺服器沒有你的金鑰時——密碼；該密碼只用於這一次連線，不寫入任何地方。
  已對真實伺服器驗證：金鑰交換、驗證、subsystem 請求、列目錄、`canonicalize` 與
  連線重用（`src/fs/tests/sftp_live.rs`，設定 `JTF_SFTP_HOST` 後執行）。側邊欄的
  已存連線與寫入端屬第二階段。
- macOS 檔案右鍵選單加入「分享…」：使用系統本身的分享面板
  （`NSSharingServicePicker`）處理選取的檔案。「服務」選單本身無法從 Qt 的檢視取得，
  這一點如實記錄而非假造（`src/ui/qt6/cpp/platform/share.h`）。
- ADR-0004 提出 SFTP 支援方案：分兩階段進行，採金鑰與 ssh-agent 驗證，主機金鑰對照
  `~/.ssh/known_hosts` 驗證，並明列哪些操作可以、哪些不可能在遠端執行。待決定。

### 修正

- **縮圖快取的上限是張數,不是容量。**`QCache` 預設每筆成本算 1,所以「4096 張縮圖」
  代表 4096 張任意大小的圖:以格狀檢視 72 像素的邊長計算約 84 MB 的 pixmap,邊長更
  大就更多。現在改成以位元組計算預算,能放幾張就放幾張。
- **資料夾樹會吃掉字母鍵。**`QTreeView` 對字母的反應是它內建的輸入即找,而且跳到符
  合項目時會把所在分支展開——所以焦點在樹上時,按 `V` 變成把樹收合展開而不是檢視檔
  案,按 `T` 也不是切換這棵樹。單鍵模式下,字母在哪個元件上都是命令;檔案清單早就
  為此關掉了輸入即找,現在樹也一致了。
- **Windows 上 session 被寫進暫存目錄。**路徑是用 `HOME` 照 macOS 的形狀拼出來的,
  而 `HOME` 在 Windows 通常沒設,於是全部落到 `%TEMP%\Library\Application Support`
  ——一個 macOS 形狀的路徑,放在系統隨時可以清空的目錄裡。分頁、標記、視窗位置隨時
  可能被清掉。現在改用平台自己的位置:Windows 用 `%APPDATA%`、Linux 用
  `$XDG_CONFIG_HOME`,macOS 路徑與先前相同。
- **Windows 版沒有程式圖示**,而且資料夾樹的根被標成 `\`——Windows 根本沒有這種路
  徑。圖示與版本資訊改由資源檔攜帶(那才是 Explorer 和工作列實際會讀的東西),根目
  錄的名稱改成問 shell,所以中文版顯示「本機」、英文版顯示 "This PC",而不是我們猜
  的字串。磁碟機會像 Explorer 一樣在名稱旁顯示代號。
- 選單列有了快捷鍵:`Alt-F`、`Alt-E`、`Alt-V`、`Alt-G`。
- **只要有第二個視窗停在根目錄，按左就不能回上一層。**每個視窗的上一層、上一頁、
  下一頁都是依「目前作用中的面板」決定啟用與否，而不是依自己擁有的面板。開了兩個
  視窗、另一個停在 `/` 時，「這裡能不能往上」問的是那個面板——於是離根目錄很遠的
  這個視窗，上一層變灰了；又因為按鍵觸發的正是選單和工具列的同一個動作，左鍵也跟
  著死掉。現在每個視窗只問自己擁有的面板。
- **鍵盤快速鍵視窗列出的是 `command.file.attributes` 而不是 屬性。**橋接層回傳的
  是字典**鍵**，指令面板會去查表，這個視窗沒有——所以每一列、每個分類標題以及鍵盤
  模式那行顯示的都是原始識別字，英文介面下也一樣。
- **重新命名等單行輸入對話框仍是 "Cancel" 與 "OK"。**它們靠搜尋 Qt 對話框內部的按
  鈕盒來換掉字，搜尋落空時就無聲地退回 Qt 的英文。現在對話框由我們自己組出來，按
  鈕不可能是別的。輸入欄也加寬到足以讀完一個檔名，且重新命名時只選取主檔名、不含
  副檔名，與系統本身的重新命名一致。
- **清單寫「種類」、檢視面板寫「類型」，而且連答案都不一樣。**欄位問的是平台，有在
  地化；面板問的是 Qt 自己的 MIME 資料庫，只有英文——所以同一個檔案，清單顯示
  「Zip封存檔」，旁邊的面板卻寫 "Compressed Archive File"。現在兩者走同一條查詢，
  也統一叫「種類」。
- **欄位永遠不會為了容納內容而變寬。**調整流程只會把欄位縮小，所以一開始就窄的欄位
  無論視窗拉多寬都維持原樣:「修改日期」停在被截斷的 "2023-12-11 …"，右邊空著一大
  片，而名稱欄把多出來的寬度全吃掉了。
- **十六個指令沒有圖示**,其中包括 複製到、移動到、以終端機開啟、熱鍵提示列 以及兩
  個切換面板的項目,在選單裡留下一排參差的空缺。現在只要有任何已註冊的指令沒有圖
  示,測試就會失敗。
- **複製分頁複製到錯的分頁,在伺服器上則什麼都沒複製到。**它是從「目前作用中的」分
  頁讀出路徑,再讓新分頁導航過去——所以在別的分頁上按右鍵,複製到的是作用中那個;而
  遠端位置根本沒有本機路徑可讀。現在改成複製你按右鍵的那個分頁,連它指向哪裡一起
  複製。
- **分頁列的右鍵選單作用在目前的面板,而不是它自己的面板。**其他所有進入面板的途徑
  都會先讓該面板成為作用中,只有這裡沒有,於是「新增分頁」開到別的面板去了。
- **遠端面板上,選單列與鍵盤仍然提供只對本機有意義的指令。**右鍵選單一直都會在遠端
  項目上略過 快速查看、在檔案管理員中顯示、以終端機開啟、分享、剪貼簿 與 移到資源
  回收筒,但另外兩條路徑通往同一批處理函式,而且都沒有檢查。現在集中在一個沒有任何
  路徑繞得過的地方停用。其中最嚴重的是檢視面板:它拿**伺服器**上的路徑去讀本機磁
  碟,遠端的 `/etc/hosts` 會預覽成這台機器的。
- 語言名稱改為「繁體中文」,不再寫「台灣繁體中文」。
- **連上伺服器後路徑列是空的，左邊的資料夾樹也不會跟著變。**兩者都向核心問「這個
  面板在哪個資料夾」，而核心回答的是**本機**路徑——遠端位置根本沒有本機路徑，所以
  兩者拿到的都是空字串。路徑列因此什麼都不畫；資料夾樹拿到空字串就直接不動，把上
  一個本機資料夾繼續highlight著，看起來就像「樹沒有跟著變」。現在拆成兩個問題：本
  機路徑（對伺服器仍然是空的，這正是書籤與相對路徑輸入需要的答案），以及顯示用路
  徑（對伺服器是 `sftp://user@host/path`）。當面板所在之處無法在樹中表示時，樹會清
  掉選取，而不是指著一個面板早已離開的資料夾。
- **熱鍵提示列列出了兩個並不存在的指令**（`file.copy`、`file.execute`）。因為沒有
  熱鍵的指令是被略過而不是畫成空白，它們就這樣無聲消失，連一個缺口都看不到。現在
  有測試會把提示列指名的每一個 id 對照指令表檢查。
- **單鍵模式下，提示列的複製與移動顯示的是 `Ins` 與 `Shift-C`**，但實際的鍵是單獨
  的 `C` 與 `M`。它列的是雙面板版本的指令；現在改列會詢問目的地的版本，也就是那兩
  個單鍵——提示列本來就該以單鍵優先。
- 測試平行執行時，一個 symlink 測試可能失敗。該檔案裡的兩個測試都用行程編號加上時
  鐘來命名暫存目錄，而相隔幾個指令的兩次呼叫可能讀到同一個奈秒值——於是兩者寫進同
  一個目錄，各自數到了對方的檔案。
- 目錄資料送達時，所有視窗都會更新清單。先前每個視窗各自有一個計時器去取同一份
  應用程式狀態，先觸發的那個把資料取走並回報「有更新」，其餘視窗被告知「沒有變化」
  ——對取資料而言是真的，對清單而言是假的。一直輸掉這個競賽的視窗就只顯示 `..`，
  而它的狀態列（直接讀自核心）卻數著它沒有顯示出來的數千個項目。
- 輸入文字的對話框（新增資料夾、新增檔案、重新命名、依樣式標記、字型名稱）按鈕
  已改為使用者的語言並附圖示。先前這些由 `QInputDialog::getText` 產生，其「OK」與
  「Cancel」來自 Qt 自己的翻譯檔，而本程式並未隨附該翻譯檔。

## [0.1.0] — 2026-08-30

第一個編號版本。以下項目都已完成並可運作；這裡記錄的是起點，而不是相對於前一版的
變更清單——因為前面沒有版本。

### 瀏覽

- 可遞迴分割的面板，每個面板有自己的頁籤，並會在下次啟動時還原，包含從頁籤拉出來
  的獨立視窗。
- 側邊欄提供資料夾樹狀清單、喜好項目、磁碟區與最近使用。
- 詳細清單與圖示檢視共用同一份資料模型與同一組選取狀態。
- 導覽路徑列平常以美觀形式顯示，點空白處即變成可編輯的完整路徑，與 Windows 檔案
  總管的行為一致。
- 欄位會依可用寬度調整：名稱欄保有可讀的下限，容不下的欄位會整欄隱藏，而不是全部
  擠成刪節號。
- 「篩選」（僅目前資料夾）與「搜尋」（含所有子資料夾）是兩件不同的事；搜尋進行中
  會浮現卡片顯示進度並提供取消。

### 鍵盤

- 兩套鍵盤設定檔皆為資料而非程式碼：**單鍵命令模式**（依 CView 鍵表）與**原生模式**
  （依作業系統慣例）。可從工具列或熱鍵切換，並有滑動指示。
- 底部熱鍵提示列，內容由目前的鍵盤設定檔與語言檔即時組出，並隨光棒所在項目改變。
  提供完整顯示、精簡顯示與操作時淡出三種模式。
- 每個功能都有命令 ID；選單、工具列、命令面板與鍵盤設定檔指向的都是同一個。

### 檔案

- 複製、移動、製作副本、重新命名、批次重新命名、新增檔案與資料夾、透過系統本身的
  介面移到資源回收筒（因此 Finder 的「放回原處」可用），以及需確認的永久刪除。
- 複製與移動可選擇目的地：列出所有已開啟的頁籤，也可以直接輸入路徑或瀏覽選擇。
- 檔案操作會排入佇列依序執行，並有視窗列出佇列、顯示進行中的進度、可取消或移除
  等待中的工作。
- 資料夾大小依需求計算，並以資料夾本身的修改時間為依據快取。
- 壓縮檔內容可像資料夾一樣瀏覽——僅限列出內容；解壓縮尚未實作
  （見 `docs/adr/0003-archive-extraction.md`）。

### 檢視

- 文字與十六進位檢視工具採串流方式建立行位置索引，因此開啟數 GB 的 log 不需要把
  整個檔案讀進記憶體。
- 預覽面板會跟著光棒移動，並等游標停下才讀檔，避免按住方向鍵時每一列都去讀檔。
- macOS 上支援快速查看（Quick Look）。

### 介面

- 淺色與深色佈景主題來自同一組語意化色彩代號；調色盤以外的程式碼不出現任何色碼。
- 英文與正體中文，預設依系統語言並可自行指定，切換不需重新啟動。
- 使用 Iconoir 圖示集並依主題著色，另有少量自行繪製的圖示補其不足。

### 已知缺口

- 尚未實作壓縮檔的解壓縮與建立。
- 尚未整合系統原生的「服務」與檔案總管右鍵選單。
- Windows 與 Linux 的檔案類型資料庫、資源回收筒與快速查看仍為佔位實作；終端機開啟
  三個平台皆已實作。
